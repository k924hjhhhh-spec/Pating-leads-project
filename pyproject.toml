[project]
name = "painting-leads"
version = "0.1.0"
description = "South Florida painting lead marketplace agent prototype"
requires-python = ">=3.10"
dependencies = [
  "openai-agents>=0.2.0",
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.8.0",
]

[tool.uv]
package = false
